[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "chainforge"
version = "0.1.0"
description = "A mini LangChain built from scratch"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["chainforge*"]